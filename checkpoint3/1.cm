/*
 * 1.cm - Valid factorial benchmark
 * Exercises assignments, arithmetic, while, and built-in I/O.
 * Expected runtime output for input 5: 120
 */

void main(void) {
  int x;
  int fac;

  x = input();
  fac = 1;

  while (x > 1) {
    fac = fac * x;
    x = x - 1;
  }

  output(fac);
}
